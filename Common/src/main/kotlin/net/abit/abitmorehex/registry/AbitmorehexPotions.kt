// Still in Registry.kt (or a separate Potions.kt)

import net.abit.abitmorehex.Abitmorehex.MODID
import net.minecraft.world.effect.MobEffects
import net.minecraft.world.effect.MobEffectInstance
import net.minecraft.world.item.Items
import net.minecraft.world.item.alchemy.Potions
import net.minecraft.core.Registry
import net.minecraft.resources.ResourceLocation
import net.minecraft.world.item.alchemy.Potion
import net.abit.abitmorehex.effects.GraspingVines


object AbitmorehexPotions {
/*
    // Basic “Whispering Tonic” — duration 3 min, amplifier 0
    val WHISPER_TONIC: Potion = Registry.register(
        Registry.POTION,
        ResourceLocation(MODID, "whisper_tonic"),
        Potion(
            Potions.AWKWARD,  // base potion
            MobEffectInstance(Registry.GraspingVines.get(), 20 * 60 * 3, 0)
        )
    )

    fun registerRecipes() {
        // If you want brewing recipes, use Fabric API’s BrewingRecipeRegistry
        // or Forge’s BrewingRecipeRegistry via a mixin or API call.
    }
    */
}
